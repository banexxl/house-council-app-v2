import { Box } from '@mui/system'

const Page = async () => {
     // const { data: location } = await supabase
     //      .from('tblBuildingLocations')
     //      .select('*')
     //      .eq('id', params.id)
     //      .single()

     // if (!location) {
     //      notFound()
     // }

     return (
          <Box>
               aaaaaaaaa
          </Box>
     )
}

export default Page

