export interface Vehicle {
          _id: string;
          location: string;
          latitude: number;
          longitude: number;
          temp: string;
          startedAt: string;
          departedAt: string;
          arrivedAt: string;
}
